const Add = () => {
  return (
    <div className="new-movie-container">
      <button type="button" className="btn-add" onClick={open}>
        + ADD MOVIE
      </button>
    </div>
  );
};

export default Add;
